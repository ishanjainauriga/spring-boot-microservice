version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
#    networks:
#      - micro_network
#
#networks:
#  micro_network:
#    driver: bridge
